# templates/backend/pyproject.toml
[project]
name = "backend-template"
version = "0.1.0"
description = "Plantilla base profesional para FastAPI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []